# PROGRESS — Vin Datathon 2026

Cập nhật lần cuối: 2026-07-27

**Cấu trúc thư mục** (sau reorg): mỗi phase 1 folder riêng trong `EDA_Insight/` — `phase0_qc/`, `phase1_descriptive/` (kèm `data/` chứa CSV trung gian), `phase2_diagnostic/`, `phase3_dashboard/` (kèm `data/`). Chart tách theo `output/phase0-3/`. Lineage đầy đủ ghi ở `EDA_Insight/DATA_LINEAGE.md`.

**Notebook trình bày (2026-07-21):** mỗi script phân tích `.py` có 1 `.ipynb` kèm (cùng thư mục, cùng tên) chart hiện inline — 9 notebook đã chạy thật qua Jupyter, 0 lỗi, 46 ảnh baked; bản đồ script↔notebook ở `EDA_Insight/NOTEBOOKS.md`. Notebook trung thực (savefig/CSV chạy lại ra byte y hệt, 0 file tracked bị sửa). ℹ️ Phát hiện phụ: chart `output/phase5/36_*.png` KHÔNG tái lập được từ `verify_target_and_cohort.py` (script này thuần tính số, không có code vẽ) — chart do session Phase 5 vẽ ad-hoc, là lỗ hổng reproducibility nhỏ của Phase 5.

**Hạ tầng (2026-07-21): đã giải quyết 2 cảnh báo lặp lại nhiều phase.** Cài thêm vào `.venv` (+ `requirements.txt`): `scikit-learn`, `scipy`, `statsmodels`, `xgboost`, `lightgbm` (cần `brew install libomp` kèm theo trên macOS), `nbconvert`, `nbclient`, `notebook`. Đã chạy lại **cả 4 notebook** (`EDA_khoi_dong`, `EDA_quan_sat`, `EDA_diagnostic`, `EDA_demographics`) qua Jupyter kernel thật (`jupyter nbconvert --execute --inplace`) — 0 lỗi, output đã bake sẵn trong file. Không còn bị chặn hạ tầng cho Giai đoạn 7-8 (ensemble/ARIMA).

| # | Giai đoạn | Agent | Trạng thái | File output | Ghi chú |
|---|---|---|---|---|---|
| 0 | Data quality & schema QC | de | ✅ done | `EDA_Insight/phase0_qc/phase0_full_qc.py`, `phase0_qc_report.md` | 0 BLOCKER, 10 WARNING, 8 INFO. Key: sales.Revenue=gross kể cả cancelled (lệch net ~24%); 18.6% dòng bán dưới giá vốn → 382 ngày lỗ gộp; web_traffic chỉ 1 dòng/ngày toàn site (không dùng traffic_source phân kênh) |
| 1 | EDA Descriptive (trend, CAGR, seasonality, Tết) | da | ✅ done | `EDA_Insight/phase1_descriptive/descriptive_summary.md` + notebook + `data/{tet_yearly_stats,daily_revenue_gross_net,category_region_decompose}.csv` | Structural break 2019 (revenue -38.6%, profit giảm nhanh gấp 3 revenue). Tết KHÔNG đồng nhất qua năm (bác giả thuyết ban đầu). Streetwear 79.9% doanh thu nhưng margin không cao nhất. ✅ Notebook đã chạy full qua Jupyter thật 2026-07-21, 0 lỗi |
| 2 | Diagnostic (revenue giảm, margin, COGS) | da | ✅ done | `EDA_Insight/phase2_diagnostic/diagnostic_summary.md` + `diagnostic_revenue.md` (mục 9-15) + `data/{yearly_kpi_decompose,promo_intensity_by_year,monthly_unit_price_ratio,category_below_cost_rate,category_region_margin_controlled,region_margin_mix_decomposition,channel_revenue_margin_by_year,tet_promo_overlap}.csv` | Break 2019 = khối lượng sụp (qty -40%, khách -28%), KHÔNG phải giá (ASP +2.4%)/mix/promo. Promo gây 47.9% dòng bán dưới giá vốn (vs 0.18% không promo) — cơ chế trực tiếp 382 ngày lỗ gộp. West margin cao ~40% do mix, ~60% do giá thật. Tết x promo: corr yếu (+0.65, n=10). ⚠️ Root cause thật của sụp khối lượng 2019 (tại sao khách rời đi) NGOÀI phạm vi data giao dịch hiện có — đã loại trừ giá/mix/promo/kênh/vùng nhưng chưa tìm được nguyên nhân gốc (giới hạn dữ liệu, không phải hạ tầng — không tự giải quyết được). ✅ Notebook đã chạy full qua Jupyter thật 2026-07-21, 0 lỗi |
| 3 | Dashboard A: RFM, cohort, marketing, demographics | da | ✅ done | `EDA_Insight/phase3_dashboard/dashboard_rfm_cohort.md` + `customer_demographics.py/.md` + `data/{rfm_customer_segments,cohort_retention_matrix,churn_risk_scores}.csv` | Pareto: 25.6% khách (Champions) = 62.9% giá trị. Cohort retention PHẲNG ~3% mọi tháng (nghi ngờ đặc tính data mô phỏng — DS phải xác nhận ở Phase 5). West region nhỏ nhất nhưng chất lượng khách tốt nhất. ℹ️ Logistic regression bản gốc viết tay bằng numpy (lúc đó venv thiếu sklearn/scipy) — nay đã cài sklearn (2026-07-21), kết quả cũ vẫn hợp lệ/không cần chạy lại, sklearn chỉ cần cho model MỚI về sau nếu muốn |
| 4 | Prescriptive: đề xuất hành động định lượng | da + ba | ✅ done | `EDA_Insight/phase4_prescriptive/prescriptive_analysis.md` + `ba_action_spec.md` (spec ba tổng hợp CHUNG cho cả Phase 4+5, đầu vào cho Phase 10) + `data/{floor_price_simulation_overall,floor_price_simulation_by_category,winback_scenario_by_segment,west_margin_replication_by_category_region,west_margin_replication_summary}.csv` | 4 đề xuất định lượng: (1) Floor price=cogs → margin +2.36pp, +0.462 tỷ VND toàn kỳ 10 năm (quick win lớn nhất, giả định KHÔNG co giãn cầu — cận trên lý thuyết). (2) Win-back Can't Lose Them/At Risk — kịch bản minh hoạ (+5/10/15pp), số tuyệt đối nhỏ (2.9-24.9 triệu VND), cần A/B test thật. (3) Nhân rộng pricing West → chỉ +38 triệu VND/10 năm, giá trị chính là bài học vận hành không phải đòn bẩy tài chính. (4) KHÔNG đề xuất giảm giá/tăng khuyến mãi (đã chứng minh không phải vấn đề giá) — ưu tiên acquisition West + retention Champions, không cắt kênh nào. ⚠️ Chưa có ba tổng hợp thành spec hành động chính thức |
| 5 | Chốt giả định model (A → B) | ds + ba | ✅ done | `EDA_Insight/phase5_model_assumptions/model_assumptions.md` + `data/{target_reconcile_sample,cohort_retention_by_first_order}.csv` (phần ba nằm chung ở `phase4_prescriptive/ba_action_spec.md` Phần 3+4) | Xác nhận ĐỘC LẬP target = gross demand (MAPE 0.000% vs sales.csv, đối chứng NET lệch 13.9%). Chốt 5 giả định G1-G5: dummy `is_post_2019` (giữ nguyên 1 tập train 2013-2022, không tách riêng); `month`+`is_low_margin_season` (2 mùa vụ khác cơ chế: lượng vs margin); `days_from_tet` liên tục; KHÔNG dùng cường độ promo tương lai (leakage); region loại khỏi model chính. 🔴 **Phát hiện quan trọng:** verify độc lập lần 2 xác nhận cohort retention PHẲNG (~5-6%, std 0.4pp, M1≈M12) — dấu hiệu mạnh dữ liệu khách hàng được sinh random độc lập theo thời gian, không có hành vi quay lại thật → feature dựa trên hành vi khách lặp lại (CLV, rolling active customers...) VÔ NGHĨA cho forecast, Giai đoạn 6 phải ưu tiên tuyệt đối feature thuần lịch. ✅ Cảnh báo hạ tầng ĐÃ GIẢI QUYẾT 2026-07-21: sklearn/scipy/statsmodels/xgboost/lightgbm đã cài, không còn chặn Giai đoạn 7-8 |
| 6 | Feature engineering thuần lịch | de + ds | ✅ done | `EDA_Insight/phase6_features/` — de: `build_features.py` + `feature_pipeline.md` + `data/{features_train(3833×21),features_forecast(548),tet_dates_extended}.csv`; ds: `screen_features.py` + `feature_spec.md` + `data/{feature_target_correlation,redundancy_correlation_matrix,tet_dates_ds_check}.csv` | 17 feature lịch: month(+sin/cos), dow(+sin/cos), quarter, day_of_month, is_post_2019(+transition), days_from_tet, is_low_margin_season, trend_index(neo 2012-07-04), 3 cờ lễ VN. **Orchestrator reconcile PASS:** train 3833 khớp sales.csv (target join lệch 0.0), forecast 548 khớp sample_submission (không leak target), 0 NaN, feature khớp 100% def ghim, 2 bản Tết de-vs-ds lệch 0 ngày mọi năm 2012-2025, days_from_tet đúng dấu+liên tục. Sàng lọc corr: mạnh nhất `month_cos`(-0.50)/`is_post_2019`(-0.38)/`days_from_tet`(0.37); dư thừa month≈quarter(0.97) → bỏ quarter; đa cộng tuyến trend_index≈year(0.996)≈is_post_2019(0.84) → linear chỉ chọn 1; cờ lễ VN + dow_cos tín hiệu yếu/nhiễu → test residual ở P7. Khuyến nghị cân nhắc loại 2012 (năm cụt, thiếu T1-6). ℹ️ Lệch nhỏ: de làm 3 cờ lễ tách, ds sàng bằng 1 cờ gộp — ma trận de (3 cờ) là bản dùng cho P7 |
| 7 | Baseline models | ds | ✅ done | `EDA_Insight/phase7_baseline/build_baselines.py` + `baseline_report.md` + `data/{metrics_summary,holdout_predictions,forecast_B1..B3b,submission_best_baseline,b3_ols_coefficients}.csv` + `output/phase7/07_*.png` (5 chart, mỗi cái có note) | 3 baseline (+1) cho Revenue/COGS độc lập, holdout chính 548 ngày (2021-07→2022-12). **Bất ngờ quan trọng: B1 seasonal naive THẮNG áp đảo** — WAPE Revenue 25.18% / COGS 23.09%; B3 linear TỆ hơn ~2× (WAPE ~47%/42%) do `trend_index` toàn cục ngoại suy lệch +29-39% (bias hệ thống, residual lệch âm). B3 còn cho 4 ngày Revenue ÂM → phải floor 0/log. **Mốc P8 phải vượt: WAPE < 25.18% (Rev), < 23.09% (COGS).** Verify: submission 548 dòng đúng format, không NaN/âm, không leakage (train_fit cắt trước holdout). ⚠️ B1 dùng chain-prediction cho đuôi >365 ngày → phần thắng hơi lạc quan ở đoạn cuối. Khuyến nghị P8: tree-based (RF/GBM, không ngoại suy tuyến tính), thêm feature `lag_365`, trend piecewise, rolling-origin backtest |
| 8 | Model theo quý + ensemble, forecast 548 ngày | ds | ✅ done | `EDA_Insight/phase8_model/build_model.py` + `model_report.md` + `data/{backtest_metrics,holdout_predictions,feature_importance,forecast_548,final_choice}` + `output/phase8/08_*.png` (4 chart) | **VƯỢT MỐC GĐ7 cả 2 target** trên fold chính `main_548d` (548 ngày holdout 2021-07→2022-12): Revenue WAPE 25.18%→**22.71%** (-9.8% rel), COGS 23.09%→**19.79%** (-14.3% rel). Model cuối = **LightGBM global + ensemble 50/50 với B1 seasonal naive** (giống cho cả 2 target, không chia quý — per-quarter tệ hơn). Feature mới `lag_365`/`lag_365_smooth7` (khuyến nghị GĐ7). Rolling-origin 5 fold, không leakage (train_fit cắt trước holdout mọi fold, production fit toàn 2012-2022). Forecast 548 ngày (2023-01-01→2024-07-01) đúng format `Date,Revenue,COGS`, không NaN/âm. ⚠️ Rủi ro cần GĐ9 xử: (a) chain lag ~183 ngày đuôi horizon (nửa đầu 2024) dùng giá trị model tự dự đoán → sai số cộng dồn CHƯA đo tách 1-365 vs 366-548; (b) không có ground-truth 2023-2024, WAPE chỉ từ holdout nội bộ; (c) fold `rolling_2` (break 2019) mọi model đều tệ 44-63% (giới hạn data); (d) GradientBoosting ổn định hơn qua fold cũ nhưng không chọn (chỉ so fold main) — cân nhắc lại làm dự phòng |
| 9 | Validation + explainability + check submission | tester + ds | ✅ done | `EDA_Insight/phase9_validation/` — `validation_report.md`, `t91_format_check.py` (QA), `explain_robustness.py` (DS), `data/{shap_summary,horizon_degradation,robustness_table,weight_grid}`, `forecast_548_SUBMIT_crlf.csv` (**BẢN NỘP CHÍNH THỨC**), `committed_baseline/*`, `rerun1_stdout.log` + `output/phase9/09_*.png` (3 chart) | **GATE: GO CÓ ĐIỀU KIỆN → điều kiện đã gỡ.** QA độc lập tính lại: Revenue WAPE 22.71<25.18 ✅, COGS 19.79<23.09 ✅ (khớp GĐ7, không bịa). Leakage audit PASS, `forecast_548.csv` byte-identical qua 2 lần rerun (sha256 080c8bb4…). 🔴 1 blocker: line-ending submit=LF vs sample=CRLF → **ĐÃ SỬA**, bản nộp = `forecast_548_SUBMIT_crlf.csv` (549 CRLF, nội dung byte-identical). DS: SHAP xác nhận chiều tác động khớp GĐ6 (`lag_365`+, `trend_index`−, `days_from_tet`+); horizon đuôi (366–548, lag chain) KHÔNG suy giảm (còn tốt hơn đầu) → bỏ lag_182; robustness giữ Ensemble 50/50 (argmin validation cả 2 target), KHÔNG re-forecast. 🟡 Rủi ro phải nêu khi trình bày: **leaf-cap `trend_index`** (100% ngày forecast > max train → tree chặn phẳng, trend đóng góp hằng số → under-forecast nếu nền 2023-24 bật tăng vượt lịch sử, rủi ro 1 chiều thận trọng); không có ground-truth 2023-24; fold break 2019 mọi model tệ 44-63% (GradientBoosting giữ làm fallback nếu drift báo sốc). 🟢 Cosmetic bỏ qua: `backtest_metrics.csv` lệch 1 ULP ở RandomForest (đa luồng), không đụng bản nộp |
| 10 | Story A→B, báo cáo, phân việc Hải/Hoàng/Đăng | pm | ⬜ pending | — | Xuyên suốt |
