# M7 — ba — báo cáo (song song với M2/M4a/M4b)

Status: ✅ done (chờ PO verify + gộp vào PROCESS.md)
Ownership: CHỈ `data/README.md`. Không đụng file khác.

## Việc đã làm

1. Đọc `data/raw/*.csv` thật (14 file, path lấy từ `src/datathon/config.RAW_TABLES`) bằng Read tool
   (header + vài dòng đầu/cuối mỗi file) — không có Bash/Python trong session này nên không chạy
   `pandas.read_csv` trực tiếp, nhưng đã đọc đúng file tại đúng path config trỏ tới, không bịa cột.
2. Đếm row count thật bằng Grep (`pattern="^"`, count mode) cho cả 14 bảng — trừ 1 dòng header ra
   số liệu ghi trong `data/README.md`. Đối chiếu chéo với `docs/analysis/phase0_qc/phase0_qc_report.md`
   (714,669 order_items, 3,833 sales, 2,412 products, 646,945 orders/payments...) — khớp 100%,
   không phát hiện lệch.
3. Xác nhận coverage ngày thật (đọc dòng đầu + dòng cuối, không suy đoán):
   - `sales.csv`: 2012-07-04 → 2022-12-31 (3,833 dòng, không thiếu ngày).
   - `web_traffic.csv`: 2013-01-01 → 2022-12-31 (3,652 dòng — thiếu 181 ngày đầu so với sales).
   - `sample_submission.csv`: 2023-01-01 → 2024-07-01 (548 dòng — khớp đúng số ngày kỳ vọng Phần B).
     ⚠ Lưu ý: lần đọc đầu bị lệch offset 1 dòng (đọc thiếu dòng cuối), đã phát hiện + đọc lại đúng
     trước khi ghi vào README — không có sai số liệu trong bản final.
4. Viết `data/README.md`: data dictionary 14 bảng (tên/mô tả/cột+dtype quan sát/grain/PK/FK), sơ đồ
   quan hệ FK tổng quát, business fact bắt buộc (sales=GROSS/target B, net_revenue chỉ Phần A,
   payments.payment_value=Monetary RFM), mục "Cách lấy data" (⚠ chưa có nguồn tải chính thức trong
   repo — đã grep RESTRUCTURE_PLAN/AGENTS/PROCESS/README root, không thấy link — ghi
   **"TODO: nguồn data — hỏi PO"**, cần PO bổ sung).
5. Không đọc được `docs/brief/data_dictionary.xlsx` (.docx tương tự) trực tiếp — binary, Read tool
   báo lỗi "cannot read binary file", không có tool giải nén/parse Office trong session. Đã dùng
   thay thế: nội dung nghiệp vụ đã có sẵn, đã kiểm chứng trong `README.md` (root) và
   `docs/analysis/DATA_LINEAGE.md` + `phase0_qc_report.md` (các tài liệu này bản thân đã đối chiếu
   gốc với `data_dictionary.xlsx`/`Đề bài1.docx` ở lần QC 2026-07-20) — không bịa, có trích dẫn
   nguồn trong `data/README.md`.

## DoD tự check

- [x] 14 bảng tài liệu hóa đầy đủ (tên, cột thật + dtype quan sát, grain/PK, FK, business fact).
- [x] Cột lấy từ CSV thật qua path `RAW_TABLES` trỏ tới, có evidence (list cột mẫu) trong README.
- [x] Business fact bắt buộc (GROSS/net/Monetary) ghi rõ, có trích nguồn.
- [x] Mục "Cách lấy data" có — vì chưa rõ nguồn chính thức nên để TODO + hỏi PO (đúng yêu cầu).
- [x] Coverage thời gian 2012–2022 + sample_submission ghi thật (không giả định).
- [x] Chỉ tạo/sửa `data/README.md`. Chưa commit PROCESS.md (đúng protocol song song).

## Việc PO cần làm tiếp

- Cung cấp nguồn tải data raw chính thức → cập nhật mục 0 `data/README.md` (đang TODO).
- Verify tích hợp cùng M2/M4a/M4b khi gộp wave, rồi PO tự cập nhật row M7 trong `PROCESS.md`
  (theo protocol, ba không commit PROCESS.md khi chạy song song).

## Commit

- Dự kiến: `git add data/README.md` — conventional commit `docs(data): add data dictionary for 14 raw tables + sourcing guide`.
- Không `git add -A`, không đụng `PROCESS.md`, không sửa CSV.
